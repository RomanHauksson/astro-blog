---
import { Icon } from "astro-icon/components";
interface Props {
  link: string;
  icon: string;
}
const { link, icon } = Astro.props;
---

<a class="rounded-full p-3 bg-slate-800 hover:bg-slate-600" href={link}
  ><Icon class="" width={24} height={24} name={icon} /></a
>
