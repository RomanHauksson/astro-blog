---
import { Icon } from "astro-icon/components";
interface Props {
  link: string;
  icon: string;
}
const { link, icon } = Astro.props;
---

<a class="shadow hover:rotate-6" href={link}
  ><Icon class="" width={32} height={32} name={icon} /></a
>
